server:
  address: localhost
  port: 8081

spring:
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/project_and_employees
    username: postgres
    password: postgres

